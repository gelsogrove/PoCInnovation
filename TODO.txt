- cambiare la cartella server
- togliere la cartella nestJS per il momento
- rinominare la caretlla di post-processing
- creare struttura monorepo
- github actions per fare il build di FE e BE
- github actions per fare ii deploy delle immaagini
- levantar el docker copiare i dist e fare il push
- ogni cartella deve avere l'immagini di docker cosi viene tutto versionato.
- il docker deve aveare un docker file cosi quando parte di copia il dist
- node-red andrebbe dentro server ma come fa a sapere qual'e' il watch sia dev che prod?

monorepo

/progetto
├── /node_modules                # Cartella condivisa delle dipendenze
├── /client                       # Cartella del client
│   ├── Dockerfile                # Dockerfile per il client
│   ├── /src                      # Codice sorgente del client
│   ├── /dist                     # Cartella dist (generata dal build)
│   └── package.json              # File di configurazione di npm
├── /server                       # Cartella del server
│   ├── Dockerfile                # Dockerfile per il server
│   ├── /src                      # Codice sorgente del server
│   ├── /dist                     # Cartella dist (generata dal build)
│   └── package.json              # File di configurazione di npm
├── .github                       # Cartella per i workflow di GitHub Actions
│   └── workflows                 # Cartella per i file di workflow
│       ├── build-client.yml      # Workflow per il build del client
│       └── build-server.yml      # Workflow per il build del server
├── package.json                  # File di configurazione generale
└── README.md                     # Documentazione del progetto