{
  "name": "nextjs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:postgres": "brew services start postgresql@14",
    "stop:postgres": "brew services stop postgresql@14",
    "restart:postgres": "brew services restart postgresql@14",
    "check:postgres": "brew services list",
    "connect:postgres": "psql -U postgres",
    "prisma:studio": "npx prisma studio",
    "prisma:migrate": "npx prisma migrate dev --name initial_schema2",
    "prisma:generate": "npx prisma generate",
    "prisma:status": "npx prisma migrate status"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.18.0",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "prisma": "^5.18.0"
  }
}
